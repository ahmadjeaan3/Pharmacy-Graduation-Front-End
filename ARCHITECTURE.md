# معمارية واجهة حياة دوائية

يعتمد المشروع أسلوب **Feature-Based Architecture** المناسب لتطبيق React متوسط أو كبير. التنظيم هنا عملي: كل مجال يحتفظ بصفحاته وطلباته ومكوناته، بينما لا ينتقل إلى `shared` إلا ما تستخدمه أكثر من ميزة فعلًا.

## الشجرة المعتمدة

```text
my-app/
├── public/
│   ├── assets/app/             # صور مستخدمة في التطبيق الحالي فقط
│   └── promotional/            # مواد النشر الخاصة بالمشروع
├── src/
│   ├── app/
│   │   ├── App.jsx
│   │   ├── AppProviders.jsx
│   │   ├── AppRouter.jsx
│   │   └── routes/
│   │       ├── pageRegistry.js
│   │       ├── routeDefinitions.jsx
│   │       └── LegacyPharmacyRequestRedirect.jsx
│   ├── features/
│   │   ├── admin/
│   │   ├── auth/
│   │   ├── chat/
│   │   ├── dashboard/
│   │   ├── donations/
│   │   ├── home/
│   │   ├── medicines/
│   │   ├── notifications/
│   │   ├── organization/
│   │   ├── pharmacy/
│   │   ├── registration/
│   │   ├── settings/
│   │   └── user/
│   ├── shared/
│   │   ├── api/
│   │   ├── auth/
│   │   ├── components/
│   │   ├── config/
│   │   ├── lib/
│   │   ├── styles/
│   │   └── utils/
│   └── main.jsx
├── .env.example
├── eslint.config.js
├── package.json
└── vite.config.js
```

## مسؤولية كل طبقة

### `src/app`

طبقة تركيب التطبيق فقط:

- `App.jsx`: جذر العرض.
- `AppProviders.jsx`: React Query، Router، ومزود جلسة المستخدم.
- `AppRouter.jsx`: يركب هيكل المسارات العامة والمحمية فقط.
- `routes/pageRegistry.js`: سجل مركزي للصفحات المحملة عند الطلب.
- `routes/routeDefinitions.jsx`: تعريف المسارات وتقسيمها حسب الدور.
- `routes/LegacyPharmacyRequestRedirect.jsx`: توافق محدود مع الروابط القديمة.

لا يوضع هنا نموذج، طلب API، أو منطق خاص بميزة.

### `src/features`

كل مجلد يمثل مجال عمل واضحًا. البنية الداخلية المعتادة:

```text
feature-name/
├── api/          # العقود وطلبات الخادم ومفاتيح React Query
├── components/   # مكونات لا تستخدم خارج الميزة
├── pages/        # مكونات مرتبطة بمسار كامل
└── utils/        # تنسيقات وثوابت خاصة بالمجال
```

لا يلزم إنشاء مجلد فارغ. يُنشأ المجلد فقط عندما توجد مسؤولية حقيقية داخله.

### `src/shared`

- `api`: عميل Axios وتحويل أخطاء الخادم.
- `auth`: تخزين الجلسة والوصول إلى رمز الدخول.
- `components`: عناصر عرض عامة مثل حالات التحميل ورأس الصفحة.
- `config`: البيئة وتعريف الأدوار والتنقل العام.
- `lib`: تهيئة مكتبات عامة مثل React Query.
- `styles`: نظام التصميم والقواعد العامة.
- `utils`: دوال عامة نقية مثل تنسيق السعر والمسافة.

`shared` لا يستورد من `features`. هذه قاعدة إلزامية لمنع الاعتماد العكسي.

## خريطة المجالات

| المجلد          | المسؤولية                                         |
| --------------- | ------------------------------------------------- |
| `admin`         | لوحة الإدارة، الاعتمادات، ومراجعة مستندات المنظمة |
| `auth`          | تسجيل الدخول، حماية المسارات، وسياق المستخدم      |
| `chat`          | الجلسات والرسائل ونتائج المساعد الدوائي           |
| `dashboard`     | إطار لوحة التحكم واختيار لوحة الدور               |
| `donations`     | عروض التبرع وطلبات المساعدة للمستخدم              |
| `home`          | الصفحة العامة وصفحة المسار غير الموجود            |
| `medicines`     | الدليل المركزي وإنشاء الدواء وتفاصيله             |
| `notifications` | مركز الإشعارات والعداد المختصر                    |
| `organization`  | ملف المنظمة والحملات والمراجعات والدليل العام     |
| `pharmacy`      | الملف والموقع والدوام والمخزون والطلبات           |
| `registration`  | إنشاء الحسابات الثلاثة وتدفق حقول كل نوع          |
| `settings`      | بيانات الحساب المشتركة وتغيير كلمة المرور         |
| `user`          | الملف الصحي والبحث والخريطة وطلبات الدواء والسجل  |

## اتجاه الاعتماد

```text
main → app → features → shared
              │
              └── تكاملات مجال محددة ومعلنة
```

التكاملات المقبولة حاليًا:

- `dashboard` يركب لوحات الأدوار ويعرض الإشعارات.
- `registration`, `settings`, و`notifications` تستخدم سياق `auth`.
- `donations` تستخدم دليل `medicines` لاختيار الدواء.

أي مكوّن عرض تستخدمه ميزتان مستقلتان يجب نقله إلى `shared/components` بدل استيراده من ميزة إلى أخرى.

## قواعد التسمية

- الصفحات: `SomethingPage.jsx`.
- المكونات: اسم وصفي بصيغة PascalCase.
- ملفات API: `featureApi.js`، ومفاتيح الاستعلام بجانب الطلبات.
- أدوات التنسيق: `featureFormatters.js` إن كانت خاصة، أو `shared/utils` إن كانت عامة.
- لا تستخدم أسماء عامة مثل `helpers.js`, `data.js`, أو `component.jsx`.

## إضافة ميزة جديدة

1. أنشئ مجلدًا تحت `src/features` باسم المجال.
2. ضع عقد الخادم في `api` والصفحات في `pages`.
3. اجعل المكونات الخاصة داخل الميزة نفسها.
4. سجّل الصفحة في `app/routes/pageRegistry.js` ثم أضف المسار والدور في `routeDefinitions.jsx`.
5. لا تنقل شيئًا إلى `shared` قبل وجود استخدام حقيقي من ميزتين.
6. نفذ `npm run check` قبل إنهاء العمل.

## إدارة الأصول

- الصور الفعالة فقط داخل `public/assets/app`.
- الشعار الملون المعتمد هو `public/assets/app/brand/dawaai-logo-color.png`، وتوجد نسخته البيضاء في المجلد نفسه للخلفيات الداكنة.
- لا تحفظ ملفات ZIP أو صورًا غير مستخدمة داخل `src` أو مسار أصول الإنتاج.

## الأمان

- المتصفح يحتوي فقط على `VITE_API_BASE_URL`.
- أسرار JWT وGoogle Places تبقى في الخادم.
- حماية المسار تحسن تجربة الاستخدام، لكن Backend يظل مسؤولًا عن الصلاحيات.
- عند استجابة `401` تمسح الجلسة والذاكرة المؤقتة للاستعلامات.
- لا تسجل كلمات المرور أو رموز الدخول في السجل أو واجهة الأخطاء.

## معيار القبول قبل الدمج

```powershell
npm run check
```

ويجب أن تنجح الخطوات التالية: ESLint، Prettier، وبناء Vite للإنتاج.
